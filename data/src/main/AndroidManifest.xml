<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!--
        Phase 12 P12.5 — TransferWorker has been deleted and the transfer
        engine now runs as `dev.ori.app.service.TransferEngineService` in
        the `:app` module. WorkManager is still initialised by the
        application's Configuration.Provider for potential future workers,
        but `:data` no longer contributes any foreground service entries.
    -->
    <application />

</manifest>
